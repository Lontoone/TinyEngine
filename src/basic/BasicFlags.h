#pragma once
#ifndef BASIC_FLAGS
#define BASIC_FLAGS

const enum class EXECUTE_TIMING
{
	MAIN_LOGIC,
	BEFORE_FRAME,
	AFTER_FRAME,

};

const enum class Bind_Type 
{
	DIFFUSE,
	NORMAL,
};

#endif // !BASIC_FLAGS
