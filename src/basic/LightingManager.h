#pragma once
#include <iostream>
#include <vector>
//#include <Hierarchy.h>
#include <Light.h>
static class LightingManager
{
public:
	static void render_to_shadowmap();
	static void request_shadowmap_uniform();

private:

};
